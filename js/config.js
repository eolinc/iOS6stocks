/**
 * Configurazione del frontend — unico file da modificare quando il sito è
 * ospitato su GitHub Pages (che serve solo file statici e non può eseguire
 * il backend Node in /server).
 *
 * Lascia "" per usare la modalità dimostrativa locale (nessun dato reale).
 *
 * Per dati reali: pubblica il backend proxy su un servizio che esegua
 * codice server-side gratuitamente (Cloudflare Workers è il più semplice,
 * vedi /server/worker.js e il README), poi incolla qui l'URL pubblico che
 * ottieni, es:
 *   window.BORSA_API_BASE = "https://borsa-proxy.tuonome.workers.dev";
 */
window.BORSA_API_BASE = "https://holy-fire-e514.infoeolweb.workers.dev";
